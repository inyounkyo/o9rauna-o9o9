# pure react project

# berow project install step by step

# explanation > prompt

npm create vite@latest
current-path > .
select > javascript
node_modules local dir install > npm install

> npm run dev
